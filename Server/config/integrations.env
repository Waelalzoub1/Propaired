# Integrations configuration for Checkr + Stripe.
# Fill the values and restart the server.

# Base URL for building redirect/webhook links.
APP_BASE_URL=

# Checkr hosted apply
CHECKR_API_KEY=
CHECKR_PACKAGE_ID=
CHECKR_WEBHOOK_SECRET=
# Optional override (default: https://api.checkr.com/v1)
CHECKR_BASE_URL=

# Stripe (Connect Express required for payouts + balances)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
# Set true to allow payment testing without a connected helper account.
STRIPE_DEV_BYPASS=true
# Options: express (default), standard, custom. Standard is forced to express.
STRIPE_CONNECT_TYPE=express

# Helper payout fee percent (integer or decimal).
HELPER_FEE_PERCENT=25
# Customer service fee percent.
CUSTOMER_SERVICE_FEE_PERCENT=5
# Sales tax percent applied to the job amount.
SALES_TAX_PERCENT=0
# Legacy fallback for fee percent.
PLATFORM_FEE_PERCENT=25

# Optional background-check vendor label
BACKGROUND_CHECK_VENDOR=
